[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-p no:cacheprovider"
markers = [
    "unit: tests sin Spark, ejecutables en local",
    "integration: tests con Spark, requieren entorno Databricks",
]